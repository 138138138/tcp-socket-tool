module.exports = {
  // dbConnectStr: 'mongodb://127.0.0.1/tcpserver',

  // base path where logger files are generated
  loggerBasePath: '',

  // use hex format to receive tcp socket
  tcpHex: true,
};
